
<script>

        // define a handler
        function doc_keyUp(e) {

            // this would test for whichever key is 78 and the ctrl key at the same time
            if (e.altKey) {
               switch(e.keyCode ) {
                 case 82:  // R
                    console.log('RANDOM')
                    document.getElementById('random_notes').click();
                    break;
               }
            }
        }
        // register the handler
        document.addEventListener('keyup', doc_keyUp, false);

</script>

   <span style="margin-right:8px;font-weight:bold"> Articles</span>
   <%= link "Lead", to: "/site/#{@site}?tag=lead_article", class: "btn btn-default btn-s", style: "background-color:cornflowerblue; color:white", id: "lead" %>
   <%= link "More", to: "/site/#{@site}", class: "btn btn-default btn-s", id: "more_articles" %>
   <%= link "Random", to: "/site/#{@site}?random=one", class: "btn btn-default btn-s", id: "more_articles" %>

   <span class="hidden-xs" style="margin-left:10px;"><%= link "Random site", to: "/random_site", class: "btn btn-default btn-s", id: "more_articles" %></span>

   <span class="hidden-xs" style="float:right;margin-top:-12px;">

       <%= form_for @conn, public_path(@conn, :site),[name: :data], fn f -> %>
              <%= text_input f, :site, value: @site, placeholder: "Site - example: demo", autocorrect: "off", autocapitalize: "off", style: "display:inline-block; margin-top:-18px; margin-bottom:3px; width:190px; height:35px; font-size:1.8rem; background-color:#dffddf;", id: "search_notes" %>
       <%= end %>

   </span>



<div style="height: 2px; background-color:#ddd; margin-top:18px;margin-bottom:1em;"></div>


<%= render "_table.html", conn: @conn, notes: @notes, id_string: @id_string, site: @site, style: "clear:both" %>


<script>
    document.getElementById("table_div").style.height = (window.innerHeight - 250) + 'px'
    console.log("window.innerHeight: " + window.innerHeight)
</script>