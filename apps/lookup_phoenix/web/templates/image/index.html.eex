<h4><%= link "Upload new image", to: image_path(@conn, :new) %></h4>

<div style="height: 600px; overflow: scroll;">
    <%= for image <- @images do %>
      <img src="<%= LookupPhoenix.ImageUploader.url({image.image, image})%>" height=150px><br>
      <p><%= image.id %>, <%= image.owner_id %>, <%= image.title %>, <%= LookupPhoenix.ImageUploader.url({image.image, image})%></p>
      <br><br>
    <% end %>
</div>